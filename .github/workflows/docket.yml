
name: docker

on:
  push:
    branches:
      - main
    pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    services:
     web:
        build: .
        ports:
         - "8080:8080"
      redis:
         image: "redis:alpine"      
